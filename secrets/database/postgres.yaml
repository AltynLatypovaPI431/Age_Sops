# Конфигурация базы данных PostgreSQL
database:
  engine: postgresql
  host: db-prod.internal.company.com
  port: 5432
  username: app_user
  password: "SuperSecureP@ssw0rd!2024"
  name: production_database
  ssl_mode: require

backup:
  enabled: true
  schedule: "0 2 * * *"
  retention_days: 30
  encryption_key: "BackupEncryptionKey123!"
